import { LoginForm } from "@/components/formularios/LoginForm"

export const LoginPage = () => {

    return(
        <div className="w-lvw flex justify-center items-center">
            <LoginForm />
        </div>
    )
}